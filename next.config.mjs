/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export: produces a plain HTML/CSS/JS bundle in ./out that can be
  // uploaded to any web host (e.g. Hostinger shared hosting) — no Node.js
  // server required.
  output: "export",
};

export default nextConfig;
